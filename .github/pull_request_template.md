## What does this change?

<!-- A sentence or two. If it adds a level or art, say which one. -->

## Checklist

- [ ] `npm run typecheck` passes locally (the project has no test runner — this is the correctness gate)
- [ ] `npm run dev` still loads and the screens I touched work
- [ ] No secrets, API tokens, or `.env` files are included
- [ ] I called out any new dependencies or `package-lock.json` changes below

## New dependencies / build changes

<!-- List any added packages or changes to build scripts, vite.config.ts, or
     wrangler.jsonc. These run in the deploy pipeline when merged + promoted, so
     they get extra scrutiny. Write "none" if there are none. -->

none
