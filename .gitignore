node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build artifacts
*.tsbuildinfo

# Dev server / local runtime caches — never check in
.vite
.wrangler

# Visual Studio local folder
.vs/

# Raw art / asset drops — staging only, not for the repo
_drop/
