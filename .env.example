# Optional. When unset, the app uses a localStorage-backed mock backend so it
# runs end-to-end with no external services. Set these to enable the real
# leaderboard via Supabase.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
