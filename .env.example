# No client-side env vars are required.
#
# Backend persistence is handled server-side by the Cactus Clans Worker
# (worker/index.ts) backed by Cloudflare D1 — configured via wrangler.jsonc,
# not via Vite env vars. In `npm run dev` the Worker is not running, so the
# app falls back to a localStorage-only mock automatically.
